# Cloudflare Pages Configuration
# This file is used for Cloudflare Workers/Pages deployment

name = "dinein-malta"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# Pages configuration
pages_build_output_dir = "dist"

# Environment variables are set in Cloudflare Dashboard:
# - VITE_SUPABASE_URL
# - VITE_SUPABASE_ANON_KEY
# Note: GEMINI_API_KEY is only used server-side in edge functions, not in client builds

# Note: For Cloudflare Pages, use the dashboard or wrangler pages commands
# This config is primarily for Workers if needed in the future

